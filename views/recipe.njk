 {% extends "layout.njk" %}

 {% block head %}
 <title>{{ item.title }}</title>

 <link rel="stylesheet" href="/recipe.css">
 {% endblock %}

 {% block content %}

 <div class="recipe">
   <div class="recipe-header">
     <img src="{{item.image}}" alt="{{item.title}}">
     <h1>{{item.title}}</h1>
     <h2>por {{item.author}}</h2>
   </div>

   <div class="recipe-content">
     <div id="ingredients" class="topic">
       <div class="button">
         <h3>Ingredients</h3>
         <h4>ESCONDER</h4>
       </div>
       <div class="topic-content" id="topic-content-ingredients">
         <ul>

           {% for ingredient in item.ingredients %}
           <li>{{ingredient}}</li>
           {% endfor %}

         </ul>
       </div>
     </div>

     <div id="preparation" class="topic">
       <div class="button">
         <h3>Modo de Preparo</h3>
         <h4>MOSTRAR</h4>
       </div>
       <div class="topic-content" id="topic-content-preparation">
         <ul>

           {% for preparation in item.preparation %}
           <li>{{preparation}}</li>
           {% endfor%}

         </ul>
       </div>
     </div>

     <div id="information" class="topic">
       <div class="button">
         <h3>Informacoes Adicionais</h3>
         <h4>MOSTRAR</h4>
       </div>
       <div class="topic-content" id="topic-content-information">
         <p>{{item.information}}</p>
       </div>
     </div>
   </div>
 </div>

 {% endblock %}